Task 8 Evidence — CLI Process Manager
Date: 2026-03-09

Implemented files:
- host/src/cli.ts (new)
- host/src/services/process-manager.ts (new)
- host/src/index.ts (updated to execute CLI dispatcher)
- tests/host/process-manager.test.ts (new)

Implemented command set:
- serve
- start
- stop
- status
- open
- rotate-token

Lifecycle/contract highlights:
- start uses detached Bun.spawn("bun run <entrypoint> serve"), appends stdout/stderr to server.log, waits for /healthz (5s timeout, 100ms poll)
- stale PID cleanup + ps command verification before trusting PID ownership
- stop sends SIGTERM, waits up to 2s, escalates SIGKILL if still alive, removes server.pid
- status returns 0 only for running+healthy; stopped/stale/unhealthy returns 1
- serve writes server.pid + runtime.json and removes server.pid on SIGINT/SIGTERM
- rotate-token uses TokenService.rotate() and prints token file path only

Test execution:
- Command: bun test tests/host/process-manager.test.ts
- Result: PASS (10 tests, 0 fail)

Project checks:
- Command: bun run check
- Result: PASS (typecheck + shell syntax)

LSP diagnostics on changed files:
- host/src/services/process-manager.ts: clean
- host/src/cli.ts: clean
- host/src/index.ts: clean
- tests/host/process-manager.test.ts: clean
