RUN 1 (GLOBAL):
Configured global Git credential helper chain:
git-credential-hostproxy

Proxy URL default: http://host.docker.internal:18765
Protocol filter is configured on the host side

RUN 2 (GLOBAL):
Configured global Git credential helper chain:
git-credential-hostproxy

Proxy URL default: http://host.docker.internal:18765
Protocol filter is configured on the host side

GLOBAL HELPERS (after twice, should have NO duplicates):
git-credential-hostproxy

CLEANING GLOBAL CONFIG FOR LOCAL TEST:

RUN 3 (LOCAL --REPO with spaces):
Configured local Git credential helper chain for /workspaces/host-git-cred-proxy/temp-scenario-2/repo with spaces:
git-credential-hostproxy

Proxy URL default: http://host.docker.internal:18765
Protocol filter is configured on the host side

LOCAL HELPERS in repo with spaces (should exist):
git-credential-hostproxy

GLOBAL HELPERS (should be EMPTY):
(empty)
